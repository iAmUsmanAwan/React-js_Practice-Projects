import InputBox from "./InputBox"

//? Highly recommended for big projects, to import all the files in one file and then export to main file ie (App.jsx). In export we can send all the imported files by seperating them with comma

export { InputBox}